# Optional: public API origin when the Express server is not same-origin (no trailing slash).
# Dev with Vite proxy: leave empty and use http://localhost:5173 with API on :5000 via proxy.
# VITE_API_URL=https://api.yourdomain.com
