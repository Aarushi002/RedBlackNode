PORT=5000
CLIENT_ORIGIN=http://localhost:5173
MONGODB_URI=mongodb://127.0.0.1:27017/redblacknode

# Phase 6 — email (choose one approach)
# SMTP_* for Nodemailer
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=noreply@yourdomain.com
MAIL_TO=redblacknode@gmail.com
# Set to "true" only if your SMTP port expects TLS (e.g. 465)
SMTP_SECURE=false

# Client (Vite) — only if the API is on another origin (e.g. api.yourdomain.com)
# VITE_API_URL=https://api.yourdomain.com
